{% unless found %}
<div class="layout">
  {% if team_badge %}
  <div class="flex flex--col gap--xsmall no-shrink">
    <img class="image-dither" src="{{ team_badge }}" height="128"{% if team_badge_invert %} style="filter: invert(1);"{% endif %}>
  </div>
  {% endif %}
  <div class="flex flex--col no-shrink">
    <span class="value">No game found</span>
  </div>
</div>
{% else %}
<div class="layout">
  <div class="flex flex--col gap--xsmall no-shrink">
    <img class="image-dither" src="{{ away_team_badge }}" height="128"{% if away_team_badge_invert %} style="filter: invert(1);"{% endif %}>
  </div>
  <div class="flex flex--col gap--xsmall">
    <span class="value value--small">@</span>
  </div>
  <div class="flex flex--col gap--xsmall no-shrink">
    <img class="image-dither" src="{{ home_team_badge }}" height="128"{% if home_team_badge_invert %} style="filter: invert(1);"{% endif %}>
  </div>
  <div class="flex flex--col no-shrink">
    {% render "date_time", start_utc_timestamp: start_utc_timestamp, utc_offset: trmnl.user.utc_offset %}
  </div>
</div>
{% endunless %}
